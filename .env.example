# World ID Configuration
# Get these from the Worldcoin Developer Portal (https://developer.worldcoin.org/)
NEXT_PUBLIC_WLD_APP_ID="app_..." # Your World ID App ID
NEXT_PUBLIC_WLD_ACTION_ID="your_action_id" # Your World ID Action ID (or "" if using nullifier_hash directly)

# Google reCAPTCHA Configuration
# Get these from the Google Cloud Console (https://console.cloud.google.com/security/recaptcha)

# Server-side Secret Key (Keep this secret! Used only in the backend API route)
RECAPTCHA_SECRET_KEY="your_recaptcha_secret_key"

# Client-side Site Key (Prefix with NEXT_PUBLIC_ to expose to the browser)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="your_recaptcha_site_key"

# Optional: Set verification priority ('worldid' or 'recaptcha')
# If 'worldid', it will prioritize World ID verification. If that fails or isn't provided, it falls back to reCAPTCHA.
# If 'recaptcha', it will prioritize reCAPTCHA. If that fails or isn't provided, it falls back to World ID.
# Defaults to 'worldid' if not set.
# VERIFICATION_PRIORITY="worldid" 